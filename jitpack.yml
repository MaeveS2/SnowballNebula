# configuration file for building releases with jitpack.io
jdk:
  - openjdk16
  
before_install:
  - wget https://github.com/sormuras/bach/raw/master/install-jdk.sh
  - source ./install-jdk.sh --feature 16 --license GPL
